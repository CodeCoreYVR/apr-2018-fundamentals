// methods - a way to do something, process
// method acting
// behaviour

// method - a function thats belongs to an array
let shoppingList = ["eggs", "milk", "chesse", "beer"];
shoppingList.length; // length property
shoppingList.pop(); // pop the array - removes that last element
shoppingList.push("coconut water");

// manipulate the beginning of the array
shoppingList.shift();
shoppingList.unshift("egg whites");
shoppingList.sort();
shoppingList.length;

// https://www.w3schools.com/jsref/jsref_sort.asp
